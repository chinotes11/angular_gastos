// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  ApiUrl: 'https://gastos-back-dot-dev-zzas-portal.ue.r.appspot.com/api/v1/',
  ApiUrlAdmin: 'https://admin-back-dot-dev-zzas-portal.ue.r.appspot.com/api/v1/',
  //ApiUrl: 'https://prov-gastos-back-dot-proveedores-303021.ue.r.appspot.com/api/v1/',  
  serverLog: 'http://localhost:4300'
  
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
